package com.example.habitick.ui.theme

import androidx.compose.ui.graphics.Color

// 定义我们的蓝色系颜色
val PrimaryBlue = Color(0xFF2196F3)      // 主色
val PrimaryDarkBlue = Color(0xFF1976D2)  // 深色主色（用于暗黑模式或强调）
val LightBlueContext = Color(0xFFBBDEFB) // 浅蓝色（用于辅助）
val BackgroundWhite = Color(0xFFF5F5F5)  // 背景灰白